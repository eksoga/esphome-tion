substitutions:
  node_name: test
  project_branch: next
  project_source: ../../components
  time_platform: sntp

packages:
  energy: !include ../../packages/tion_energy.yaml
  debug: !include ../../packages/debug.yaml
  mqtt: !include ../../packages/mqtt.yaml
  auto: !include ../../packages/tion_auto_co2.yaml
  entities: !include pkg_entities.yaml

mqtt:
  broker: localhost

tion:
  restore_state: true
  auto:
    co2: fake_co2

sensor:
  - platform: template
    id: fake_co2
    lambda: return 800;

